services:
  etl:
    image: weather-etl:latest
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      RUN_ONCE: "false"  # continuous loop by default; set to "true" for single run
    volumes:
      - ./data:/app/data
    restart: unless-stopped
